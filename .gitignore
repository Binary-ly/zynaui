node_modules/
dist/
!docs/dist/
.DS_Store
*.log
vendor/
composer.lock
composer.json
.vscode/
*.php
# docs/zynaui.css is a build artifact but is committed on purpose: deploy.yml
# subtree-pushes docs/ as-is, so the deployed site needs the compiled file.
playwright-report/
test-results/

# Scratch / social assets — not for commit
UI-RESEARCH.md
docs/social-6-genres.html
screenshot-social.cjs
zyna-6-genres.png
